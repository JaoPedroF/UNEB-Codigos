using System;

class Program {

  static void Main() {

    int X, Y, D;
    int i, j=0;

    Console.WriteLine("Valor de X: ");
    X = int.Parse(Console.ReadLine());

    Console.WriteLine("Valor de Y: ");
    Y = int.Parse(Console.ReadLine());

    Console.WriteLine("Valor de D: ");
    D = int.Parse(Console.ReadLine());

    for(i = X, j=0; i<=Y; i = i + D)
    j = j + 1;

    Console.WriteLine("Número mínimo de pulos é: {0}", j);
  }
}
